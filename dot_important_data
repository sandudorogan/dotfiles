alsamixer
nmcli dev wifi connect "" password ""
bluetoothctl
# Devices data:
xinput
libinput # Can record then repeat actions.
# transmission remote cli: 
tremc
#
find / -name '*.desktop' -exec grep -H 'Image Viewer' {} \; 2>/dev/null
# Update fonts cache:
fc-cache -f -v
